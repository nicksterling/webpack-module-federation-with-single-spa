{
  "name": "module-federation",
  "private": true,
  "workspaces": {
    "packages": [
      "home",
      "shopping",
      "header",
      "nav",
      "utils"
    ]
  },
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^6.0.1"
  }
}
